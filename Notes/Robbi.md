There's 2 prefabs for Robbi, one's just the model the other has the parent object, sphere, and model all configured. 

Robbi the model should have the RobbiController.cs script attached. assign the sphere and robbi as targets

The camera should have the ThirdPersonCamera.cs script and the target should not be the parent object but the Robbi the model not the parent object
